server:
  port: 9001
  servlet:
    context-path: /order-server
spring:
  application:
    name: order-server
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
#management:
#  endpoint:
#    web:
#      exposure:
#        include:'*'